<br><br>
<div class="container">

    <div class="row">
        <div class="col-12">
            <h4>{{title}}</h4>
        </div>
    </div>

    <div class="row">
        {{#each products as |product|}}
            <div class="col-3">
                {{> card/render item=(use product as='card')}}
            </div>
        {{/each}}
    </div>

</div>
