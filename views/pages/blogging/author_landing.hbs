{{#contentFor "body"}}

    <div class="container">
        <div class="row">
            <div class="col-12">
                <h1>Authors</h1>
            </div>

            {{#each authors as |author|}}
                <div class="col-4">
                    {{> card/render item=(use author as='card')}}
                </div>
            {{/each}}
        </div>
    </div>

{{/contentFor}}

{{> commonpage/page }}
